schema_version: "1.1"
name: cursor-unknown-format
description: Cursor transcript 的欄位對不上。skill 要回報實際欄位並說明無法解析，不能捏造使用者需求。
tags: [degrade]
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 15
  timeout_seconds: 300
  allowed_tools: [Read, Glob, Grep, Skill]
