# get.chezmoi.io drops its binary here when run from inside the source dir
bin/

# tests/sandbox 的探針輸出（.gate/l9-*）。
.gate/

.claude/settings.local.json
CLAUDE.local.md

# EnterWorktree 建立的隔離 worktree，內容是同 repo 的另一個 checkout
.claude/worktrees/

# python bytecode (tools/*.py are run in place)
__pycache__/
*.pyc
