# repo-only files that must never land in $HOME
README.md
init.sh
AGENTS.md
CLAUDE.md
CONTEXT.md
docs/
specs/
tests/
.scratch/

{{ if eq .chezmoi.os "windows" -}}
# Windows uses PowerShell 7 + Oh My Posh; these are Unix-only source targets.
.zshrc
.zprofile
.p10k.zsh
.config/nvim/
{{ else -}}
AppData/
{{ end -}}

# Oh My Zsh runtime state, so `exact = true` on .oh-my-zsh does not delete it.
# `dir/**` matches the directory itself as well as everything beneath it.
.oh-my-zsh/cache/**
.oh-my-zsh/log/**

.git
